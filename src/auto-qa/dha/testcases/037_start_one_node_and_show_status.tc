bug #741
===
prepare clean env

var master slave
var sip
var nic

kill {master} ha service
kill {slave} ha service

set {master} log checkpoint
set {slave} log checkpoint
start {slave} ha service
wait {slave} log "---------- ActionMySQL HA ----------", timeout=3min

sleep 2s

get status

assert status has `
	+-- {slave}({nic})
    +-- HA Service: Starting
`

rollback:
start {master} ha service
wait {master} log "Node is running", timeout=3min
wait {slave} log "Node is running", timeout=3min