prepare clean env

var master slave

stop {master} ha service

set {master} log checkpoint
start {master} ha service
wait {master} log "Node is running", timeout=2min

get status
assert status all ok

assert {master} is master

assert {slave} is slave

rollback: